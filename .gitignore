# Rust / Cargo
target/
Cargo.lock.bak
*.rs.bk
**/*.rs.bk

# IDEs
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
*.swp
*.swo
*~
# Editor / OS temp files (e.g., VSCode autosave, Dropbox conflict-copy artifacts)
*.tmp
*.tmp.*

# Coverage
*.profraw
*.profdata
tarpaulin-report.html
lcov.info

# SBOM / release artifacts (generated locally during release dry-runs)
/dist/
*.sbom.json
*.spdx.json

# Sigstore signatures (committed only via release.yml)
*.sig
*.crt

# Local config (per-developer)
.cargo/config.local.toml
.envrc
.env
.env.local

# Test scratch
/tmp/
/scratch/

# Default store root (ADR-0036: `./papers` under the CWD). Running doiget
# — or the test suite — inside the repo materialises this; it is never
# repo content. Issue #406.
papers/
